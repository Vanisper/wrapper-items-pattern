{
  "name": "wrapper-items-pattern",
  "private": true,
  "type": "module",
  "author": {
    "name": "Vanisper",
    "email": "273266469@qq.com",
    "url": "https://github.com/Vanisper"
  },
  "packageManager": "pnpm@11.1.2",
  "engines": {
    "node": ">=22.22"
  },
  "scripts": {
    "predev": "pnpm --filter './packages/**' build",
    "dev": "pnpm --filter @playground/vue dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm predev && tsc --noEmit && pnpm --filter @playground/* typecheck"
  },
  "devDependencies": {
    "@types/node": "catalog:dev",
    "tsdown": "catalog:dev",
    "typescript": "catalog:dev",
    "vitest": "catalog:dev"
  }
}
