# AGENTS.md

## 基本说明

本项目基础语言为中文。

## 项目定位

`wrapper-items-pattern` 提供面向 wrapper/items 组件模式的 headless 基础能力。

`@wrapper-items/core` 聚焦 item 集合与顺序管理：

- item 注册、更新、注销
- 注册顺序与逻辑顺序
- collection snapshot
- snapshot 订阅
- 注册、注销、重排后的 collection/order 归一化

wrapper/items 组件场景通常还会出现 active、selected、focused、visible、multi-selection、keyboard navigation 等 UI 行为。例如 Swiper 需要处理 active slide、visible slide、循环切换和导航，Tabs 需要处理 active tab、焦点与键盘访问。

这些 UI 行为的抽象不进入 collection/order 基础层。不同组件对同一个词的语义和策略并不一致，应由使用方、具体组件或可组合行为模块实现，避免基础控制器为了适配不确定场景而膨胀。

## 中文书写

本节记录项目中中文内容的通用书写原则，适用于源码注释、README、设计文档、测试标题等场景。不同载体有不同写作目的，不应把源码注释规则机械套用到文档正文。

### 基本原则

由于编码语言、API、类型系统和工具链基本以英文为主，中文插入英文代码环境时，视觉重量和阅读节奏会明显不同。
因此源码注释应力求精炼、简洁，只说明必要契约、约束、边界和原因，避免用中文复述代码。

README、设计文档等 Markdown 正文是正常文章语境，应按文章自身的结构和表达目的使用中文标点。源码 JSDoc 中“不以句号结尾”的倾向只适用于注释场景，不适用于文档正文。

### 列表标点

中文列表项的标点根据列表项的语法结构和上下文连贯性决定：

- 列表项是短语、词组、名词结构或简短分句时，末尾通常不加标点
- 列表项是完整句子、包含多个分句，或内部已经使用逗号、分号等标点时，末尾可以使用句号
- 同一组列表应保持结构统一，不要混用“有的加句号、有的不加句号”的写法

源码 JSDoc 通常要求精炼，所以在列表场景下更常命中“短语、词组或简短分句不加标点”的规则。

## 注释风格

注释采用语言与项目基础语言对齐。

中文语言基础时，源码注释应使用中文，除非引用外部 API 名称、协议名或代码术语。

中文语境下的 JSDoc 与英文注释风格不同（相关解释见中文书写规则）：

- 中文注释一般不以句号结尾。
- 多行说明中，摘要与补充说明之间保留一个空行，保证 JSDoc 渲染时换行。
- 需要补充说明时优先使用 `@description`。
- 多个要点可使用列表，列表标点遵循中文书写规则。
- `@example`、`@default`、`@deprecated` 等标签视情况使用。

示例：

```ts
/**
 * 根据传入的 id 列表调整顺序
 *
 * @description 只调整已注册 item 的顺序，未传入的已注册 item 会保留在末尾
 */
setOrder(ids: readonly ItemId[]): void
```

如果说明较多：

```ts
/**
 * 根据传入的 id 列表调整顺序
 *
 * @description
 * - 只调整已注册 item 的顺序
 * - 未传入的已注册 item 会保留在末尾
 */
setOrder(ids: readonly ItemId[]): void
```

接口、类型、public 方法的注释优先级较高。内部实现只在存在非直观约束、边界或维护风险时加注释，避免复述代码。

公开 API 的 JSDoc 应面向三方使用者，说明参数、返回值、状态变化、失败条件、顺序规则等调用契约。
架构取舍、模块边界、某个模块“不做什么”这类内容优先写入项目文档，不要混进用户会直接读到的类型注释。

接口、类型、类、字段和方法的注释按“层级归属”和“单向依赖”判断放置位置：

- 总注释说明当前结构整体成立、且可被下级成员共同复用的语义，例如类型角色、类职责、整体不变量、字段之间的关系。
- 字段和方法注释说明成员自身的语义、取值范围、特殊状态、默认值、调用契约或失败条件。
- 如果一段说明只属于某个字段，不要放进总注释里列清单；字段多了以后，总注释不应该膨胀成字段说明列表。
- 如果一段说明只属于某个方法，不要放进类注释或接口注释里列清单；方法多了以后，总注释不应该膨胀成方法说明列表。
- 如果一段说明表达多个成员之间的关系，或所有成员共同依赖的约束，应该放在总注释或总描述里。
- 注释关系应从整体到局部单向展开，避免字段说明反向补足总注释，也避免同一语义在多个层级重复维护。

如果文件顶部有文件级注释，需要保证第一个导出符号不应该继承这段说明，此时要为第一个导出符号单独写 JSDoc，避免文档工具误挂注释。

## 测试要求

单元测试的 `describe`、`it` 标题语言风格与项目基础语言对齐，便于测试报告直接表达项目语义。
API 名称、变量名、错误信息、正则断言等代码语义保持原样即可。

修改 TypeScript 源码、公开类型或单元测试后至少运行：

```bash
pnpm test
pnpm typecheck
```

涉及打包配置、package exports、依赖、构建产物入口或发布文件范围时再运行：

```bash
pnpm build
```
